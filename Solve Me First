#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int solveMeFirst(int a, int b) {
 int sum;
 // Hint: Type return a+b; below:
 sum=a+b;
  return sum;
}

int main() {
  int a, b;
  int sum;
  cin>>a>>b;
  sum = solveMeFirst(a,b);
  cout<<sum;
  return 0;
}
